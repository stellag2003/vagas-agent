# matching

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build matching` to build the library.

## Running unit tests

Run `nx test matching` to execute the unit tests via [Jest](https://jestjs.io).
